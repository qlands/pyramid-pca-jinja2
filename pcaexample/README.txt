PCA Example
===============================

This is the HOST application that defines a series of interfaces and extension points that then plugins use to extend it or modify it.

To run it do:
$ python setup.py install
$ initialize_pcaexample_db ./development.ini
$ pserve ./development.ini
